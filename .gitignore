# Dependencies
node_modules/

# Build output
dist/
build/
out/
*.tsbuildinfo

# Runtime state (locks, session caps, rate buckets, audit JSONL, logs)
# Created at first run by the daemon — never commit.
data/
*.log

# Local scratch / displaced-spec backups
.scratch/

# Environment / secrets
# (config/config.json holds no secret-typed fields by schema, but keep the guard)
.env
.env.*
*.pem
client_secret_*.json

# Test coverage
coverage/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
