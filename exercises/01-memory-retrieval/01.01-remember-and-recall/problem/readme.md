# Problem: Storing and Querying User Preferences

Write a python script or tool invocation that records a user preference for dark mode and vim keybindings, then queries it back using semantic search.
